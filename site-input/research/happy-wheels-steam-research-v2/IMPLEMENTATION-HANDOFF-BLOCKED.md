# Implementation Handoff — blocked only on Repository Binding

Research Pack + 3-query SERP closeout + Content Routing Receipts are complete.

The next Codex prompt must not be generated until the exact implementation repository is confirmed.

Required binding:
- GitHub repository: exact `owner/name`
- canonical local checkout: exact path

Once bound, the implementation prompt must:
- reference `happy-wheels-steam-research-v2.zip`,
- assign Codex implementation-only ownership,
- forbid repeat external research,
- reuse the approved mature presentation baseline,
- implement Homepage + the 5 launch pages only,
- keep verified backlog folded/hidden unless needed,
- keep `RESEARCH_REQUIRED` routes unpublished,
- use existing Content Routing → Shared Writer → Publisher,
- run monitoring writeback after production deployment.
